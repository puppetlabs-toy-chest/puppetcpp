/**
 * @file
 * Contains the forward declarations in the values namespace.
 */
#pragma once

#include <memory>
#include <string>

namespace puppet { namespace runtime { namespace values {

    // Forward declaration of undef.
    struct undef;

    // Forward declaration of defaulted.
    struct defaulted;

    // Forward declaration of regex.
    struct regex;

    // Forward declaration of type.
    struct type;

    // Forward declaration of variable.
    struct variable;

    // Forward declaration of array.
    struct array;

    // Forward declaration of hash.
    struct hash;

    // Forward declaration of value.
    struct value;

}}}  // namespace puppet::runtime::values